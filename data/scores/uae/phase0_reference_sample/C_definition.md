# Reference Sample C — Definition

Generated by ``scores.analysis.phase0_reference_sample``.

## Inclusion rule

- require_sac_compliant      = True
- require_clean_sample       = True
- require_ratio_compliance   = True

A row is kept in ``C`` iff all active predicates are satisfied.

## Size thresholds

- min |C|      = 30  (framework §9.2 D3)
- min |C_s|    = 27  (framework §9.2 D5)
- min N_peer   = 9  (framework §9.2 D7)

## Observed sizes

- |C|           = 264
- sectors in C  = 7 (4 meet the min size)
- peer groups   = 7 (7 meet the min size)

## Contamination notes

The current panel does not expose a delisting flag, so the `not delisted` clause of analysis_plan_v2.md is replaced by `clean_sample == 1` (flag written by `add_clean_sample_flag` in `panel_creation/build_panel.py`). Known restated firms are not excluded — if an audit-report list becomes available, extend `ReferenceSampleSettings` with a `restated_firms` deny-list rather than filtering inline.